= form_for(@photo.comments.build, remote: true) do |f|
  = f.text_area :body, class: 'form-control', placeholder: t('comment')
  = f.hidden_field(:photo_id, value: @photo.id)
  %p.text-right
    = f.submit t('create'), class: 'btn btn-default'
